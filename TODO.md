when you add manage the following happens:
    -u -t is not working
    if you start to type in the URL and tken it goes htttopssss
    if you deploy to hangs on accountid
    once you deploy it creates a new instance in the admin